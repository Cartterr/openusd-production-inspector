# Validation Configuration

Version 0.1 will define only local, explicit configuration for naming rules and required metadata. Do not add service configuration, credentials, or organization-specific private policies.
